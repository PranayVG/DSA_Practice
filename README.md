# DSA_Practice
This my first Project

ls means list file
ls -a means hidden files
clear mean clear terminals
Untracked  means new files that git doesn't yet track
modified means changes
staged means file is ready to be commited 
unmodified means unchange

add&commit

add: adds new or changes file in your working directory to the git statging area.
git add <file name>
to change add all files do only git add .

commit: it is records the chanegs
git commit -m "Some messages"

git push command - upload local to remote
git push origin main

